[project]
name = "dingda"
version = "0.1.0"
description = "DingDa AI customer service desktop platform"
readme = "README.md"
requires-python = ">=3.13"

[dependency-groups]
dev = [
  "ruff>=0.11",
]

[tool.uv.workspace]
members = [
  "python/sidecar",
  "python/packages/contracts",
  "python/packages/shared",
  "python/packages/gateway",
]

[tool.uv.sources]

[tool.ruff]
target-version = "py313"
line-length = 100
src = ["python"]

[tool.ruff.lint]
select = [
  "E",
  "F",
  "I",
  "UP",
  "B",
  "SIM",
  "N",
]
ignore = []

[tool.ruff.lint.isort]
known-first-party = [
  "contracts",
  "gateway",
  "shared",
]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
line-ending = "lf"
