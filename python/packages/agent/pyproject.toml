[project]
name = "agent"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
  "openai>=1.60",
  "anthropic>=0.49",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/agent"]
