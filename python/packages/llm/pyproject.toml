[project]
name = "llm"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = ["model"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/llm"]

[tool.uv.sources]
model = { workspace = true }
