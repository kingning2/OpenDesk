---
description: Branch workflow and generated scope rules
alwaysApply: true
---

# 分支工作流

分支名格式：

```text
<role>/<kind>/<slug>
```

- role：`frontend`、`contract`
- kind：`feature`、`fix`、`hotfix`、`chore`、`refactor`、`docs`
- slug：kebab-case

```bash
pnpm branch:create frontend feature mail-sync
pnpm branch:create contract chore mail-schema
pnpm branch
pnpm branch:sync
```

`frontend` 覆盖 React、Tauri 与 Rust；`contract` 覆盖 `contracts/`、同步脚本及 Rust/TypeScript 生成物；`main` 和无法识别的实际分支按集成范围处理。

切换分支后必须运行 `pnpm branch:sync`，由 `skills/opendesk/scripts/branch-tools.mjs` 写入 `.cursor/rules/active-branch.mdc`。`kind=docs` 时只改文档、规则、Skill、模板和配置。

跨端字段始终先改 Contract，再运行 `pnpm contracts:sync`。配置源见 [`skills/opendesk/config/branch_roles.json`](../../skills/opendesk/config/branch_roles.json)。
