---
description: Branch workflow — create role branches and sync active-branch rules from git branch name
alwaysApply: true
---

# 分支工作流

OpenDesk 用 **分支名** 决定角色 scope 与 Cursor 约束规则。

## 命名规范（必须）

```
<role>/<kind>/<slug>
```

| 段 | 说明 | 示例 |
|----|------|------|
| `role` | 职责域 | `frontend` · `python` · `contract` |
| `kind` | 变更类型 | `feature` · `fix` · `hotfix` · `chore` · `refactor` · `docs` |
| `slug` | 任务短名 (kebab-case) | `m5-ui-shell` |

示例：`frontend/feature/m5-ui-shell`

## 创建分支

```bash
pnpm branch:create frontend feature m5-ui-shell
pnpm branch:create python fix sidecar-logging
pnpm branch:create contract chore agent-v2-schema
pnpm branch                                      # 交互式
```

## 切换分支后同步规则

```bash
pnpm branch:sync
```

生成 [`.cursor/rules/active-branch.mdc`](active-branch.mdc)。

## kind 说明

| kind | 用途 |
|------|------|
| `feature` | 新功能 / UI 骨架 / 垂直切片 |
| `fix` | 常规缺陷修复 |
| `hotfix` | 紧急热修，优先合 main |
| `chore` | 工具链、CI、脚本、规则 |
| `refactor` | 重构，不改对外行为 |
| `docs` | 仅文档与计划 |

## 角色 (role)

| role | 职责 |
|------|------|
| `frontend` | React · UI · Tauri · `crates/**` |
| `python` | `python/**` |
| `contract` | `contracts/` + codegen |
| `main` | 集成 |

配置：[`skills/opendesk/config/branch_roles.json`](../../skills/opendesk/config/branch_roles.json)

## AI 助手

1. 切换分支后运行 `pnpm branch:sync`（或确认 `active-branch.mdc` 已更新）
2. 只改「允许修改」路径；`kind=docs` 时勿改业务代码
3. 跨端字段：用 `contract/<kind>/<slug>` 或先改 `contracts/` 再 codegen
