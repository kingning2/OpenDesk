# Auto-generated path fragment for /v1/agent/ping
# Rust runtime client only — React must not call this URL.

/v1/agent/ping:
  post:
    operationId: agent_ping
    summary: "agent ping (Rust → Python)"
    tags: [agent]
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '../../schema/v1/agent/sidecar/ping.request.schema.json'
    responses:
      "200":
        description: Success
        content:
          application/json:
            schema:
              $ref: '../../schema/v1/agent/sidecar/ping.response.schema.json'
      "500":
        description: Sidecar error
        content:
          application/json:
            schema:
              $ref: '../sidecar.v1.yaml#/components/schemas/SidecarError'
