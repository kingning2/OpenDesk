{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "opendesk://runtime/ipc/llm_test_connection.request/v1",
  "title": "runtime/ipc/llm_test_connection.request",
  "description": "Test LLM connectivity from draft or stored settings. api_key empty uses keyring secret.",
  "type": "object",
  "required": ["provider", "model_id", "api_key"],
  "properties": {
    "provider": { "type": "string" },
    "base_url": { "type": "string" },
    "model_id": { "type": "string" },
    "api_key": {
      "type": "string",
      "description": "Draft API key for this probe. Empty = resolve from keyring."
    }
  },
  "additionalProperties": false
}
