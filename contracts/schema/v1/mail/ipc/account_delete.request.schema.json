{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "opendesk://mail/ipc/account_delete/request/v1",
  "title": "mail/ipc/account_delete.request",
  "description": "Delete one bound mail account (credentials and IMAP sync state are removed).",
  "type": "object",
  "required": ["id"],
  "properties": {
    "id": { "type": "string" }
  },
  "additionalProperties": false
}
