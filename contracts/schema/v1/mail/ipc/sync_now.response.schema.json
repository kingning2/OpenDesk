{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "opendesk://mail/ipc/sync_now/response/v1",
  "title": "mail/ipc/sync_now.response",
  "description": "Background job ids enqueued for IMAP sync.",
  "type": "object",
  "required": ["job_ids_json", "enqueued"],
  "properties": {
    "job_ids_json": {
      "type": "string",
      "description": "JSON array of background_job id strings."
    },
    "enqueued": { "type": "integer" }
  },
  "additionalProperties": false
}
