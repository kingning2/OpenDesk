{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "opendesk://crawler/ipc/channel_list.response/v1",
  "title": "crawler/channel_list.response",
  "description": "Tauri IPC: paginated crawler channel list.",
  "type": "object",
  "required": ["ok", "channels_json", "total"],
  "properties": {
    "ok": { "type": "boolean" },
    "channels_json": {
      "type": "string",
      "description": "JSON array of channel rows (channel_result DTO + id, job_id, keyword)."
    },
    "total": { "type": "integer" },
    "trace_id": { "type": "string" }
  },
  "additionalProperties": false
}
