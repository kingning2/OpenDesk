/** Workflow editor — Monaco wrapper (skeleton). */
export function WorkflowEditor() {
  // TODO: wire @monaco-editor/react when workflow feature is approved
  return (
    <div className="flex h-64 items-center justify-center rounded-[var(--radius-lg)] border border-border bg-muted text-[length:var(--text-sm)] text-muted-foreground">
      WorkflowEditor scaffold
    </div>
  );
}
