/// <reference types="vite/client" />

/** 编译期渠道平台 id — 由 Vite `define` 注入（见 `OPENDESK_CHANNEL_PLATFORM`）。 */
declare const __OPENDESK_CHANNEL_PLATFORM__:
  | "xianyu"
  | "xiaohongshu"
  | "douyin";
