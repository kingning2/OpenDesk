[package]
name = "adapter"
version.workspace = true
edition.workspace = true

[dependencies]
async-trait = { workspace = true }
common = { path = "../common" }
ports = { path = "../ports" }
runtime = { path = "../runtime" }
