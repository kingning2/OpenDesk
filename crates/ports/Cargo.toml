[package]
name = "ports"
version.workspace = true
edition.workspace = true
description = "Port traits — 基础设施边界（repository / sidecar / license）"

[dependencies]
async-trait = { workspace = true }
common = { path = "../common" }
thiserror = "2"
serde = { workspace = true }
